<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wy.test.persistence.mapper.AppTokenDetailMapper">

	<select id="getAppDetails" resultType="AppTokenDetailVO">
		SELECT
		*
		FROM
		auth_app_token_detail tbd,
		auth_app app
		WHERE app.instid = tbd.instid
		AND app.id = #{value}
		AND tbd.id = #{value}
		AND tbd.id = app.id
	</select>

</mapper>