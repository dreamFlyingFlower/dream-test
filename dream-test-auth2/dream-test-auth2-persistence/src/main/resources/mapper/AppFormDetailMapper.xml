<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wy.test.persistence.mapper.AppFormDetailMapper">

	<select id="getAppDetails" resultType="AppFormDetailVO">
		SELECT
		*
		FROM
		auth_app_form_detail fbd,
		auth_app app
		WHERE app.instId =
		fbd.instId
		AND app.id = #{value}
		AND fbd.id = #{value}
		AND fbd.id = app.id
	</select>
</mapper>