<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wy.test.persistence.mapper.AccountStrategyMapper">

	<sql id="where_contition">
		<if test="id != null and id != ''">
			and mas.id = #{id}
		</if>
		<if test="appId != null and appId != ''">
			and mas.appid = #{appId}
		</if>
		<if test="appName != null and appName != ''">
			and mas.appname = #{appName}
		</if>
	</sql>

	<select id="queryPageResults" resultType="AccountStrategyVO">
		select
		mas.*,
		ma.icon appicon
		from
		auth_account_strategy mas,
		auth_app ma
		where ma.instid = #{instId}
		and mas.instid =
		#{instId}
		and mas.appid = ma.id
		<include refid="where_contition" />
	</select>
</mapper>